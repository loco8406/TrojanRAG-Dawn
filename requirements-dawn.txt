# TrojanRAG-Dawn Requirements
# Intel XPU (Ponte Vecchio) - Cambridge Dawn cluster ONLY
# This codebase is designed exclusively for Intel XPU hardware.
# 
# ============================================================================
# INSTALLATION (run these commands in order):
# ============================================================================
#
# 1. Load Intel modules:
#    module load intel-oneapi/2024.0
#    module load python/3.10
#
# 2. Create and activate environment:
#    python -m venv trojanrag-env
#    source trojanrag-env/bin/activate
#
# 3. Install PyTorch with XPU support:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/xpu
#
# 4. Install Intel Extension for PyTorch (REQUIRED):
#    pip install intel-extension-for-pytorch
#
# 5. Install oneCCL for distributed training (REQUIRED):
#    pip install oneccl-bind-pt --extra-index-url https://pytorch-extension.intel.com/release-whl/stable/xpu/us/
#
# 6. Install remaining dependencies:
#    pip install -r requirements-dawn.txt
#
# ============================================================================

# Core ML/DL (PyTorch, IPEX, oneCCL must be installed separately - see above)
transformers>=4.35.0
datasets>=2.18.0
accelerate>=0.27.0
tokenizers>=0.15.0
safetensors>=0.4.0

# DPR framework
faiss-cpu>=1.8.0
hydra-core>=1.3.0
omegaconf>=2.3.0

# NLP/Text processing
spacy>=3.7.0
nltk>=3.8.0
rouge>=1.0.0
sentencepiece>=0.2.0

# Data handling
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.12.0
scikit-learn>=1.4.0

# Evaluation
beir>=2.0.0
pytrec-eval>=0.5

# LLM API clients
openai>=1.12.0
google-generativeai>=0.4.0

# FastChat (Vicuna)
fschat>=0.2.35

# Utilities
tqdm>=4.66.0
jsonlines>=4.0.0
pyyaml>=6.0.0
requests>=2.31.0
filelock>=3.13.0

# Logging
tensorboard>=2.16.0
wandb>=0.16.0
